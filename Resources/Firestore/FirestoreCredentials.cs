namespace Leux.Resources.Firestore
{
    // Firebase service account credentials are stored in Resources/Raw/leux-new-firebase-adminsdk.json
    // That file is gitignored and must be provided locally to run the app.
    // See: https://console.firebase.google.com → Project Settings → Service Accounts → Generate new private key
}
